if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/assets/<%=params[:id].to_s%>/map_associate?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'assets/map_associate')).html_safe %>");


