if (window.history.pushState!=null) window.history.pushState(null, '', '/assets?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'assets/index')).html_safe %>");
