if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'ZL on the Air', '/assets/new?<%=@parameters%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'assets/new')).html_safe %>");


