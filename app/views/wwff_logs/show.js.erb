if (window.history.pushState!=null) window.history.pushState(null, '', '/wwff_logs/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'wwff_logs/show')).html_safe %>");


