if (window.history.pushState!=null) window.history.pushState(null, '', '/wwff_logs?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'wwff_logs/index')).html_safe %>");
