if (window.history.pushState!=null) window.history.pushState(null, '', '/awards/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'awards/show')).html_safe %>");


