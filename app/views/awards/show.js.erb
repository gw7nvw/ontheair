if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/awards/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'awards/show')).html_safe %>");


