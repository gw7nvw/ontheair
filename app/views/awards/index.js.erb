if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/awards/');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'awards/index')).html_safe %>");
