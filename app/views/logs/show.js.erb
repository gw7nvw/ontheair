if (window.history.pushState!=null) window.history.pushState(null, '', '/logs/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => '/logs/show')).html_safe %>");


