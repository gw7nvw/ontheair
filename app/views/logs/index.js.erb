if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/logs?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'logs/index')).html_safe %>");
