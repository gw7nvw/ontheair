if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/callsigns/<%=params[:id]%>/edit?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'callsigns/edit')).html_safe %>");


