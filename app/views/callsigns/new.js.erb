if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, <%= raw(@site_title) %>, '/callsigns/new?<%=@parameters%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'callsigns/new')).html_safe %>");


