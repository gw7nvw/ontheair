if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'Huts on the Air', '/signup');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/new')).html_safe %>");


