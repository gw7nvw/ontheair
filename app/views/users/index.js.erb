if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/users/');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/index')).html_safe %>");
