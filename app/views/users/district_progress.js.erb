if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/users/<%=@user.callsign%>/district_progress?<%=raw(@parameters)%>');

$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/district_progress')).html_safe %>");


