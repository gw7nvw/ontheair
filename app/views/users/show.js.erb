if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/users/<%=@user.callsign%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/show')).html_safe %>");


