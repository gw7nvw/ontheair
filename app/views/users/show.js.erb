if (window.history.pushState!=null) window.history.pushState(null, '', '/users/<%=@user.callsign%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'users/show')).html_safe %>");


