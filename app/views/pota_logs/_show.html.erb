Please check that the following log is correct, and click the <b>Send Log</b> button to submit it to POTA.
<br/>
<br/>
Filename: <%=@filename%><br/>
Email to send to: <%=@address%><br/>
<br/>
<%=@pota_log%>
<br/>
<br/>
Invalid contacts rejected: <%=@invalid_contacts.count%><br/>
<% @invalid_contacts.each do |contact| %>
  <%=link_to "Contact: "+contact.id.to_s, '/contacts/'+contact.id.to_s ,remote: true, :onclick => "linkHandler('submit')"%><br/>
<% end %>
<%=link_to "Send Log", '/pota_logs/'+@park.id.to_s+"/send?date="+@logdate+"&user="+@user.callsign, class: "btn btn-small btn-primary",remote: true, :onclick => "linkHandler('submit')"%>
<%=link_to "Cancel", '/pota_logs?user='+@user.callsign, class: "btn btn-small btn-primary",remote: true, :onclick => "linkHandler('submit')"%>
