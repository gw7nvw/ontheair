if (window.history.pushState!=null) window.history.pushState(null, '', '/pota_logs?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'pota_logs/index')).html_safe %>");
