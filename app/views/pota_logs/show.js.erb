if (window.history.pushState!=null) window.history.pushState(null, '', '/pota_logs/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'pota_logs/show')).html_safe %>");


