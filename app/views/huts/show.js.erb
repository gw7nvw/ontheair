if (window.history.pushState!=null) window.history.pushState(null, '', '/huts/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'huts/show')).html_safe %>");


