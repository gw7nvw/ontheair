Please check that the following log is correct, and click the <b>Download</b> button download it. You can then upload it to <a href="https://www.sotadata.org.uk/en/" target="_blank">SOTA</a>.
<br/>
<br/>
Filename: <%=@filename%><br/>
<br/>
<% @sota_log.split('<eor>').each do |sota_log| %>
  <%=if sota_log and sota_log.length>1 then sota_log+"<eor>" end%></br>
<% end %>
<br/>
<br/>
Invalid contacts rejected: <%=@invalid_contacts.count%><br/>
<% @invalid_contacts.each do |contact| %>
  <%=link_to "Contact: "+contact[:contact].id.to_s, '/contacts/'+contact[:contact].id.to_s ,remote: true, :onclick => "linkHandler('submit')"%> -- <%=contact[:message]%><br/>
<% end %>
<% if @chaser then %>
  <% if @resubmit then resubmit_str="&resubmit=true" else resubmit_str="" end %>
  <%=link_to "Download Log", '/sota_logs/chaser.adi?user='+@user.callsign+resubmit_str, class: "btn btn-small btn-primary" %>
<% else %>
  <%=link_to "Download Log", '/sota_logs/'+@summit.safecode+".adi?date="+@logdate+"&user="+@user.callsign, class: "btn btn-small btn-primary" %>
<% end %>
<%=link_to "Cancel", '/sota_logs?user='+@user.callsign, class: "btn btn-small btn-primary",remote: true, :onclick => "linkHandler('submit')"%>
