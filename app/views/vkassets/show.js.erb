if (window.history.pushState!=null) window.history.pushState(null, '', '/vkassets/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'vkassets/show')).html_safe %>");


