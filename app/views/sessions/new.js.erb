if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'Railhead Nusery', '/signin');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'sessions/new')).html_safe %>");

