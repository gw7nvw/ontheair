if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'OnTheAir', '/about');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'static_pages/about')).html_safe %>");

