if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'Alerts On The Air', '/alerts');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'static_pages/alerts')).html_safe %>");
