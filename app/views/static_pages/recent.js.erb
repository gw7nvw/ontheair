if (window.history.pushState!=null) window.history.pushState(null, '... On The Air', '/recent?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'static_pages/recent')).html_safe %>");

