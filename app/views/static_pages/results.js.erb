if (window.history.pushState!=null) window.history.pushState(null, '... On The Air', '/results?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'static_pages/results')).html_safe %>");

