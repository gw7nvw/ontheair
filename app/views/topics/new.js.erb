if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'QRP.NZ | <%@mtitle%>', '/<%=@ptitle%>/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'topics/new')).html_safe %>");
