if (window.history.pushState!=null) window.history.pushState(null, '', "/topics/<%=@topic.id%>?<%=raw(@parameters)%>");
$('#right_panel').html("<%= escape_javascript(render(:partial => 'topics/show')).html_safe %>");
