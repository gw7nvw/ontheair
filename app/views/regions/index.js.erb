if (window.history.pushState!=null) window.history.pushState(null, '', '/regions?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'regions/index')).html_safe %>");
