if (window.history.pushState!=null) window.history.pushState(null, '', '/regions/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'regions/show')).html_safe %>");


