if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/states/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'states/show')).html_safe %>");
<% if @section %>
    window.location.hash = "<%= @section %>";
<% end %>



