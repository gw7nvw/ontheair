if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/contacts/editgrid');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/editgrid')).html_safe %>");


