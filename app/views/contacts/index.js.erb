if (window.history.pushState!=null) window.history.pushState(null, '', '/contacts?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/index')).html_safe %>");
