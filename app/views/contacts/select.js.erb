if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/contacts/select');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/select')).html_safe %>");


