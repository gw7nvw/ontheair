if (window.history.pushState!=null) window.history.pushState(null, '', '/contacts/<%=params[:id]%>/edit');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/edit')).html_safe %>");


