if (window.history.pushState!=null) window.history.pushState(null, '', '/contacts/<%=params[:id]%>/edit?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/edit')).html_safe %>");


