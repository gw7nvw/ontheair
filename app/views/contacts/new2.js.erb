if (window.history.pushState!=null) window.history.pushState(null, 'Huts on the Air', '/contacts/new2');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/new2')).html_safe %>");


