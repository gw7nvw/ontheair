if (window.history.pushState!=null) window.history.pushState(null, '', '/contacts/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/show')).html_safe %>");


