if (window.history.pushState!=null) window.history.pushState(null, '', '/contacts/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/show')).html_safe %>");


