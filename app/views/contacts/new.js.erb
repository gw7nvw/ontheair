if (window.history.pushState!=null) window.history.pushState(null, 'Huts on the Air', '/contacts/new?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contacts/new')).html_safe %>");


