if (window.history.pushState!=null) window.history.pushState(null, '', '/islands/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'islands/show')).html_safe %>");


