if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, 'Route Guides', '/password_resets/new');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'password_resets/new')).html_safe %>");

