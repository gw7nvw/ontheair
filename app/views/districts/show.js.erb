if (window.history.pushState!=null) window.history.pushState(null, '', '/districts/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'districts/show')).html_safe %>");


