if ((window.history.pushState!=null) && (site_back!=true)) window.history.pushState(null, '', '/districts/<%=params[:id].to_s%>?<%=raw(@parameters)%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'districts/show')).html_safe %>");


